#include<bits/stdc++.h> 

using namespace std;

int main (){
    int rows,cols;

    cout <<"Enter the Numbers of rows in restaagle- ";
    cin>> rows;

    cout <<"Enter the number of columns in rectangle -"
    cin >> cols;

    cout<< "Rectangle of dimenssion" << rows << "*" << cols <<  endl;

    for(int i= 0; i < rows; i++){
        for (int j =0; j < cols:j++ ){
            if 
        }
    }

}